This folder is intended for PDF fixtures (e.g., `.pdf`).

We do **not** commit binary files here. Instead, `tests/shared/preprocess_files.py` will generate example PDFs locally (and then extract text) as part of the fixture run.


