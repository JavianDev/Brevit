This folder is intended for image fixtures (e.g., `.png`, `.jpg`).

We do **not** commit binary files here. Instead, `tests/shared/preprocess_files.py` will generate example images locally (and then OCR them) as part of the fixture run.


